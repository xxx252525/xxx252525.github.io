import { Plugin, App } from 'vuepress/core';

/**
 * Options for @vuepress/plugin-register-components
 */
interface RegisterComponentsPluginOptions {
    /**
     * An object that defines name of components and their corresponding file path
     */
    components?: Record<string, string>;
    /**
     * Absolute path to the components directory
     */
    componentsDir?: string | null;
    /**
     * Patterns to match component files using [globby](https://github.com/sindresorhus/globby)
     *
     * The patterns are relative to componentsDir`
     */
    componentsPatterns?: string[];
    /**
     * A function to get component name from the filename
     */
    getComponentName?: (filename: string) => string;
}
declare const registerComponentsPlugin: ({ components, componentsDir, componentsPatterns, getComponentName, }?: RegisterComponentsPluginOptions) => Plugin;

declare const getComponentsFromDir: ({ componentsDir, componentsPatterns, getComponentName, }: Omit<Required<RegisterComponentsPluginOptions>, "components">) => Promise<Record<string, string>>;

declare const prepareClientConfigFile: (app: App, options: Required<RegisterComponentsPluginOptions>, identifier: string) => Promise<string>;

export { type RegisterComponentsPluginOptions, getComponentsFromDir, prepareClientConfigFile, registerComponentsPlugin };
